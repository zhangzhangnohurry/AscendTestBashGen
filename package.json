{
  "name": "ascend-command-workbench",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Command Workbench for reviewable execute-to-here test scripts.",
  "scripts": {
    "start": "node src/server/index.js",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
